# What the tests and the data generator need, pinned to the versions they were run on.
# The workbook build itself (model/build_workbook.py) runs on Windows with Microsoft 365
# Excel and pywin32; model/render_previews.py needs pymupdf and pillow. CI needs neither.
numpy==2.5.3
pandas==3.0.3
openpyxl==3.1.5
pytest==9.1.1
